# a. Find the difference between set1 and set2. Set to variable.
# b. Find the difference between set2 and set1. Set to variable.
# c. Create new set by joining the 2 differences (union).


set1 = {10, 20, 30, 40, 50}
set2 = {30, 40, 50, 60, 70}

set1_diff = (set1.difference(set2))
print(set1_diff)
set2_diff = (set2.difference(set1))
print(set2_diff)

new_set = set1_diff.union(set2_diff)
print(new_set)
•	Conditionals
o	If-else Flowchart
o	Three conditions
•	Elif
o	If age < 12
•	Elif
	Age >= 12
	And
	Age < 18
•	Else
	Age > 18
o	If
o	Elif
o	Else
o	Finish Grade Conditional
o	Submit Homework
